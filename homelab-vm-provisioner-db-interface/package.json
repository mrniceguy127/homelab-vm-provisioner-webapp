{
  "name": "homelab-vm-provisioner-db-interface",
  "version": "1.0.0",
  "description": "HTTP interface microservice for the provisioner PostgreSQL store (job metadata, events, resource locks)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "database",
    "postgres",
    "provisioner",
    "jobs",
    "microservice"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {}
}
